=> Indexing 
-> We know that every page has a slot array. Each slot array has every slot of that slot array, every element is pointed with the page. So now what we can do for the next element ID that we discussed, that we map indexes for all the pages, we can store a key value hash map, that is key will be the index and value can be the page number and the slot ID, that is page ID and the slot ID. So if you want the fifth row, that's in the second page, so you can set the page ID and the slot ID which points to the fifth row. This is only for psql as in innoDb we dont have the slot array as it is clustered index 
-> This approach is also not very good, but suppose as we know that if we have a key value where that key is the ID and the value is the page ID and the slot ID, so suppose the page ID, slot ID is 8 GBs and this is both GBs, that is 24 GBs, and suppose you have a billion records in 300 databases, suppose 300 tables. So that is 324 into a billion, that's 10 power 9, that's around, around hundreds of GBs of RAM, which is not feasible. So we need a better approach.
-> in ram bst is used to serach whihc is very optimal , gpt correct me whil making notes if i am worng and expalin me the correct approach in detail 
->
