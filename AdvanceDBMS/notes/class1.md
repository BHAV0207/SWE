what should the data base should be able to do 
-> store and reterieve data 
-> persist data that is when we close the application the data os not lost , and is still persisted when we reopen the system basically data should be in the disk rather than in the ram 
-> there should be concurrency control that is multiple people are able to read and write simaltenausly 
 - how is concurreny possible in sql databases ?? ans = acid properties / transactions
-> there should be a query language to access the database and perform operation on it 

expected prerequisits for next few classes 
1) java 17 (maven)
2) c++ 17 (camke)
3) testing (Junit5) and (google test)
4) valgrind / perf / visual VM
5) Docker 
6) postrgres Sql 15 

